# Additive Sheet Form changes

Sheet Forms are user-created, but v1 only supports additive changes to existing Sheet Forms: users can rename a form, add columns, and hide or deprecate columns, but cannot delete columns with data or change existing column types. This keeps user-created database-backed forms possible without building a full migration engine before the core data-entry and API path works.
